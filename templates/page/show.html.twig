{% extends 'base.html.twig' %}

{% block title %}
    {{ page.title }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{#{% block template %}#}
{#    {{ include('uploaded_templates/' ~ templateName) }}#}
{#{% endblock %}#}

{% block body %}

    <section>
        <div class="container">

            <div class="section-title">
                <h2>{{ page.title }}</h2>
            </div>

            <div class="row content">

                {# @TODO - IF widget last POST#}
                <div class="col-lg-12">
                    {{ page.content|html_entity_decode|raw }}
                </div>
            </div>

        </div>
    </section>

    {#    {{ include('uploaded_templates/' ~ templateName) }}#}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}