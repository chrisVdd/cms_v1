{% extends 'admin/base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block title %}Edit Page{% endblock %}

{% block body %}
{#    <h1>Edit Page</h1>#}

    {{ include('admin/page/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="card">
        <div class="card-body">
            <a href="{{ path('admin_page_index') }}" class="btn btn-primary" role="button">
                <i class="fas fa-chevron-left"></i> back to list
            </a>

            {{ include('admin/page/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
