{% extends 'admin/base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
            <tr>
                <th>CreateDate</th>
                <td>{{ post.createDate ? post.createDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateDate</th>
                <td>{{ post.updateDate ? post.updateDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Online</th>
                <td>{{ post.online ? 'Yes' : 'No' }}</td>
            </tr>

            <h1>IMAGE</h1>
            <img src="{{ post.imagePath|imagine_filter('squared_thumbnail_small') }}" alt="">


            {#            {% if post.imageFilename %}#}
{#                <tr>#}
{#                    <th>ImageName</th>#}
{#                    <td>#}
{#                        <img src="{{ uploaded_asset(post.imagePath)|imagine_filter('squared_thumbnail_medium') }}" alt="">#}
{#                    </td>#}
{#                </tr>#}
{#            {% endif %}#}
        </tbody>
    </table>

    <a href="{{ path('admin_post_index') }}">back to list</a>

    <a href="{{ path('admin_post_edit', {'id': post.id}) }}">edit</a>

    {{ include('admin/post/_delete_form.html.twig') }}
{% endblock %}
